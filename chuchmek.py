import header
import string
import random

from numpy import random as nprand


def gen_lake():
    lake_rad = nprand.randint(1, 5)
    lake_center = (nprand.randint(1, header.DEFAULT_WEIGHT_MAP_VILLAGE), nprand.randint(1, header.DEFAULT_HEIGHT_MAP_VILLAGE))
    lake = 
    
gen_lake()
