REQUIRED:
  python, pip3 and all packages in requirements.txt

1) Give right to run the "orpha" - main file : "chmod +x ./orpha"
2) Install python required packages: "sudo pip3 install -r requirements.txt"
3) Run the game: "./orpha"
