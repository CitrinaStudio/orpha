package inside;

import String;
import inside.Util;
import Header;
import Random;
import HaxeLow;
import Sys;
import haxe.io.Input;

class Player {
    public static function new_player() : Dynamic { //Creating new player
        Sys.print("Input name: ");

        return 0;
    };
    public static function main(): Void {
        var player_params = {name: "namf", jopa: "ass"};
        Sys.println(player_params.jopa);
    };
}
